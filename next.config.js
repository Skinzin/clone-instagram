/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["github.com", "images.unsplash.com", "upload-static.hoyoverse.com", "globaltour.starrail.info"]
  },
  reactStrictMode: true,
}

module.exports = nextConfig
